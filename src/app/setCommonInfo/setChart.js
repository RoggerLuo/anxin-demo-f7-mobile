// var echarts = require("imports?this=>window!./echart.js")
// var ctx = document.getElementById("pie-chart");
// debugger
// var myChart = echarts.init(ctx);

export default function execute(data,para) {
    // myChart.setOption(data(para));
}
